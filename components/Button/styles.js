import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        padding: 8,
        fontSize: 16,
        fontWeight: 'bold',
        borderWidth: 2,
        borderColor: '#000',
        borderRadius: 4,
        textAlign: 'center',
    },
});  